{
  "name": "zquas-edge",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for zquas.ai: Link headers, Markdown content negotiation, MIME overrides.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
